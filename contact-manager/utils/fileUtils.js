const fs = require("node:fs");

const readFromFile = (fileName) => 0;
const saveToFile = (fileName, data) => 0;
const isExistingFile = (fileName) => 0;
const createNewFile = (fileName) => 0;
// const deleteFile = (fileName) => 0;
